lob_levels: 10
horizons: [10, 50, 200]
mode: return   # direction | return
eps: 0.0          # direction阈值过滤（先0）
threshold: 0.55   # 用于hit-through/tail-loss的决策阈值

split:
  train_ratio: 0.7
  val_ratio: 0.15

model:
  logreg_C: 1.0
  logreg_max_iter: 200
